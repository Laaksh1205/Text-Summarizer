transformers
transformers[sentencepiece]
datasets
sacrebleu
#scarebleu is like evolution metrics,
#on doing text summarization we use a matrix called row matrix 
#& to use it this library is required.
rouge_score
py7zr
pandas
nltk
tqdm
PyYAML
matplotlib
torch
notebook
python-box==6.0.2
ensure==1.0.2
fastapi==0.78.0
uvicorn==0.18.3
Jinja2==3.1.2
-e .
#it will install the setup.py, 
#where we will be writing the logic of ous local package installation code